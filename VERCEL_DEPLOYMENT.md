# تعليمات رفع اللعبة على Vercel

## الخطوات:

### 1. Build اللعبة
```bash
npm run build
```

### 2. رفع على Vercel

#### الطريقة الأولى: Vercel CLI
```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول
vercel login

# رفع المشروع
vercel --prod
```

#### الطريقة الثانية: من خلال موقع Vercel
1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط على "New Project"
3. اربط حساب GitHub
4. اختر المشروع
5. Vercel سيكتشف الإعدادات تلقائياً

## الإعدادات المهمة:

### Build Settings
- **Build Command**: `npm run build`
- **Output Directory**: `dist/public`
- **Install Command**: `npm install`

### Environment Variables (إذا كنت تستخدم قاعدة بيانات)
قد تحتاج إلى إضافة:
- `DATABASE_URL`
- `SESSION_SECRET`

## ملاحظات:

### اللعبة كـ Static Site
اللعبة الحالية تعمل بالكامل على المتصفح (Client-side only)، لذلك:
- ✅ سريعة جداً على Vercel
- ✅ تعمل أوفلاين بفضل PWA
- ✅ لا تحتاج سيرفر

### الملفات الثقيلة
الملفات الثقيلة (models 3D) موجودة في:
- `dist/public/models/` (~33 MB)
- `dist/public/sounds/` (~850 KB)

Vercel يدعم ملفات حتى 50 MB لكل ملف.

### PWA Support
اللعبة تعمل كـ Progressive Web App:
- Service Worker موجود في `dist/public/sw.js`
- Manifest موجود في `dist/public/manifest.json`

## بعد الرفع:

1. افتح الرابط اللي هيبعته Vercel
2. اللعبة هتشتغل مباشرة
3. المرة الثانية هتشتغل أوفلاين!

## الدومين المخصص (اختياري)

يمكنك ربط دومين خاص من:
Settings → Domains → Add Domain

---

🎮 **اللعبة جاهزة للرفع!**
